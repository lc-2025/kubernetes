import { VERSION } from '../utils/constants';

/**
 * @description Returns a greet
 * @author Luca Cattide
 * @date 25/01/2026
 * @returns {*}  {string}
 */
const greet = (): string => `Hello from version ${VERSION}`;

export { greet };
